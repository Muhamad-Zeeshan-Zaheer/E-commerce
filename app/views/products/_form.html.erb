<div class="d-flex justify-content-center align-items-center" style="height: 100vh;">
  <div class="card" style="width: 18rem;">
    <div class="card-body">
      <h5 class="card-title"><%=@product.new_record? ? 'New Product' : 'Edit Product'%> </h5>

      <%= form_with model: @product do |form| %>
        <div class="mb-3"> 
          <%= form.label :name, class: "form-label" %>
          <%= form.text_field :name, class: "form-control",placeholder:"Enter the product name"%> 
        </div>

        <div class="mb-3"> 
          <%= form.label :description, class: "form-label" %>
          <%= form.text_area :description, class: "form-control",placeholder:"Enter the product descripiton", rows:5%> 
        </div>

        <div class="mb-3">
          <%=form.label :price, class:"form-label"%>
          <%=form.number_field :price, class: "form-control" , placeholder: "Enter the item price"%>
        </div

        <div class="text-end">
          <%= form.submit class: "btn btn-success" %> 
        </div>
      <% end %>
    </div>
  </div>
</div>